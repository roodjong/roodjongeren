# This file can be checked into version control its secrets and configuration are different from production

BACKEND_URL=http://localhost:8080/backend
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080/backend
